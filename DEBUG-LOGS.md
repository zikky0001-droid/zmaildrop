# ZMAIL Drop diagnostics

Maildrop diagnostics are server-side only. The Vercel function logs retain the full Maildrop request, HTTP status, raw response, parsed GraphQL response, GraphQL errors, message counts, delete operation, verification, and timing.

The public inbox and message pages do not render or return a debug panel.
